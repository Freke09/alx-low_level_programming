#include "main.h"
#include <stdio.h>

/**
* swap_int -a function to swap the valuesof two integers
* @a: variable to swap
* @b: variable to swap
* Return: Always 0
*/

void swap_int(int *a, int *b)
{
	*a = *b;
}
