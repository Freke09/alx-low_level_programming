#include "main.h"
#include <stdio.h>

/**
* reset_to_98 -a function that takes a pointer
* to an int as parameter, amd updates the value it points to
* @n: variable being stored
* Return: 0 Always
*/

void reset_to_98(int *n)
{
	int n = 98;

	int *p = &n;
}
