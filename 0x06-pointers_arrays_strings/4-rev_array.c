#include "main.h"

/**
* reverse_arrary - A function that reverses the content of an array of integers
* @a: The int array
* @n: The number of element in the array
* Return: Always 0
*/

void reverse_array(int *a, int n)
{
	int i;

	for (i = n; i >= 0; i--)
}
